<h1>
<% @user.posts.each do |post| %>
	<p><%= link_to post.memo, post_path(post.id) %></p>
	<p><%= attachment_image_tag(post, :image, :fill, 100, 100, format: 'jpeg',fallback: "no_image.png",size: "100x100" ) %></p>
<% end %>

</h1>

